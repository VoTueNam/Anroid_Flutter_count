name: call_api
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  encrypt: ^5.0.0
  rsa_encrypt: ^2.0.0
  pointycastle: ^3.1.1
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
